# PKOS Windows 앱 사용법

## 실행
배포된 `PKOS.exe`를 더블클릭합니다. 실행 파일에는 파이썬과 변환 엔진, 태극기·애국가 PDF 샘플이 포함되어 있어 별도 파이썬 설치가 필요 없습니다.

소스 폴더에서는 `PKOS_실행.cmd`를 더블클릭해 설치된 파이썬으로 실행할 수도 있습니다. 먼저 아래의 의존성 설치 명령을 한 번 실행하세요.

이 PC에서는 새로 만든 EXE가 Windows 애플리케이션 제어 정책(오류 4551)에 의해 차단되어 EXE 실행 검증을 완료하지 못했습니다. 파이썬 소스 앱의 GUI·변환 테스트는 통과했습니다.

## 내 자료 변환
1. 탐색기에서 원본 폴더를 엽니다.
2. 상단 주소 표시줄을 클릭하고 Ctrl+C로 전체 경로를 복사합니다. 예: `G:\내 드라이브\00_개인지식운영체계(PKOS)\01_외부 정보`.
3. 앱의 **원본 폴더** 칸에 Ctrl+V로 붙여넣습니다. 폴더 우클릭 → **경로로 복사**로 생기는 따옴표도 처리합니다. **찾아보기**로 폴더를 직접 선택해도 됩니다.
4. **결과를 저장할 폴더**를 지정합니다. 원본과 다른 위치를 사용하세요. 아직 없는 새 폴더의 전체 경로를 입력하면 변환 시 생성됩니다.
5. **폴더 확인**으로 파일 목록을 확인하고 **변환 시작**을 누릅니다.
6. 완료 후 **변환 결과** 목록에서 파일을 선택하면 오른쪽에 내용이 표시됩니다.
7. **결과 폴더 열기**로 탐색기를 열거나 **선택한 파일 열기**로 파일을 엽니다. `.md` 연결 프로그램이 없다면 메모장 등으로 여세요.

원본 파일은 수정하지 않습니다. 하위 폴더 구조를 유지하며 결과를 `.md`로 만듭니다. 재실행할 때 이미 변환한 파일은 기본적으로 건너뜁니다. 원본을 수정한 뒤 다시 변환하려면 이 옵션을 해제하세요.

## 샘플 테스트
**샘플로 테스트**를 누르면 내 파일 선택 없이 태극기·애국가 PDF를 변환합니다. 결과 목록에서 가사 내용을 확인하세요. 샘플은 `%LOCALAPPDATA%\PKOS\샘플`에 저장합니다. 원본 PDF는 그 안의 `원본` 폴더에 있습니다. 공개 가사를 비교하기 위해 샘플에만 개인정보 가리기를 적용하지 않습니다.

## 지원 범위
- 한글 HWP/HWPX, Word DOCX, PowerPoint PPTX, Excel XLSX, 텍스트형 PDF, TXT, CSV, HTML, Markdown 파일을 폴더 단위로 변환합니다.
- PDF는 글자만 추출합니다. 그림 추출·스캔 OCR은 지원하지 않습니다.
- 구글 드라이브의 G: 경로는 Drive 데스크톱 앱에서 실제로 열 수 있어야 합니다. 클라우드 전용 문서는 로컬 파일로 내려받을 때 인터넷 연결이 필요할 수 있습니다.
- 구글 문서·시트·슬라이드는 DOCX·XLSX·PPTX로 내려받거나 기존 코랩 3부를 사용하세요. 구글 공유 링크·폴더 ID·`/content/drive/...` 경로를 PC 앱에 넣지 마세요.
- 이 앱은 코랩 2부의 폴더 변환 기능을 기반으로 합니다. 블로그 PDF의 글 단위 분리는 코랩 1부를 사용하세요.

## 개인정보와 중지
개인정보 가리기는 기본으로 켜져 있습니다. 자동 탐지는 오탐과 누락이 있을 수 있으므로 결과를 확인하세요. 파일 이름·경로는 결과 메타데이터에 남을 수 있습니다. 앱에서 새로 만드는 개인정보 보고서는 가리기 전 원본 값을 포함하지 않도록 설정했습니다.

**중지**를 누르면 현재 파일을 마친 뒤 멈춥니다. 완료한 결과는 유지됩니다. 경로 설정은 `%LOCALAPPDATA%\PKOS\settings.json`에 저장되어 다음 실행 때 복원됩니다.

## 소스에서 실행·제작
```sh
python -m pip install -r requirements.txt
python pkos_app.py
```

Windows EXE 제작:
```sh
python -m pip install pyinstaller
python build_windows.py
```

결과: `dist/PKOS.exe`. PDF 샘플은 기존 저장소 파일을 그대로 포함합니다. PDF 자체를 재생성할 때만 reportlab과 한글 TTF가 추가로 필요합니다.

# 내 기록 → 마크다운 변환기

[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/legoschool/pkos-fileconverter/blob/main/PKOS_%EB%B3%80%ED%99%98%EA%B8%B0.ipynb)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

**블로그·한글·워드·PPT·엑셀·구글문서**에 흩어진 내 기록을,
AI가 읽기 좋은 **마크다운(.md)** 으로 한 번에 모아주는 도구입니다.

설치도, 회원가입도, 서버도 없습니다. **위 배지를 누르면 바로 시작**합니다.

> PKOS(개인지식운영체계) 프로젝트

---

## 왜 만들었나

우리는 지식과 경험이 가장 중요한 시대를 살지만, 정작 그것을 관리하는 체계는 없습니다.
내 기록은 외장하드·원드라이브·네이버 블로그·메모·유튜브·노션·구글드라이브에 흩어져 있고,
그렇게 조금씩 사라집니다.

AI 시대에는 **AI가 가장 잘 읽어낼 수 있는 형식**으로 바꾸어 두는 일이 필요합니다.
그리고 특정 도구에 얽매이지 않는, **내 것인 체계**여야 합니다.
사라지지 않는 기억, 되살아나는 기억으로 남도록.

이 도구는 그 첫 단계 — **흩어진 기록을 한 형식으로 모으는 일** — 을 맡습니다.

---

## 무엇을 변환하나

| 부 | 대상 | 형식 |
|----|------|------|
| **1부** | 네이버 블로그 백업 | `.pdf` → **글 한 편씩** 따로 분리 |
| **2부** | 문서 폴더 통째로 | `.hwp` `.hwpx` `.docx` `.pptx` `.xlsx` `.pdf` `.html` `.txt` `.csv` |
| **3부** | 구글 문서 | 구글 문서 · 시트 · 슬라이드 → 모두 `.md` 로 |

**한글 `.hwp`** 는 한컴오피스 없이, 외부 프로그램 없이 순수 파이썬으로 읽습니다.

---

## PC에서 창 프로그램으로 사용하기

`PKOS.exe`를 실행해 원본 경로를 붙여넣고 결과 폴더를 선택하면 됩니다. 샘플 테스트, 진행 상태, 중지, 결과 미리보기와 폴더 열기를 제공합니다.

[Windows 앱 상세 사용법](WINDOWS_사용법.md) · 소스로 실행: `python pkos_app.py`

## 쓰는 법

1. 위 **[Open In Colab]** 배지 클릭
2. 맨 위 **준비하기** 두 칸 실행 → 구글 드라이브 연결 허용
3. 필요한 부(1·2·3)로 가서 **▶ 버튼**을 순서대로 클릭

- **1부**: 폴더 링크·ID 또는 경로 / **2부**: 폴더 경로 / **3부**: 폴더 링크·ID
- **2부 경로 입력 상세 안내:** [코랩에서 폴더 경로 복사하기](경로_복사_안내.md)
- 새 PKOS 버전은 코랩에서 복사한 전체 경로와 Windows 내 드라이브 경로도 처리합니다.
- 중간에 끊겨도 다시 누르면 **이어서** 진행
- 한 파일이 실패해도 나머지는 계속 변환

> **드라이브 연결 승인 창은 반드시 직접 클릭**해야 합니다.
> 브라우저가 팝업을 막으면 연결이 진행되지 않습니다.

### 내 파일 없이 샘플로 먼저 테스트하기

1. 새 PKOS 노트북에서 **준비하기 두 칸**을 실행합니다.
2. **먼저 연습하기 → ▶ 샘플 PDF로 변환 테스트**를 누릅니다. 경로 입력은 필요 없습니다.
3. 태극기와 애국가 1~4절·후렴이 들어 있는 2쪽 PDF가 생성되고, 2부와 같은 변환기로 마크다운으로 바뀝니다.
4. 셀 아래에서 가사가 읽히는지 확인합니다. 원본은 왼쪽 📁의 `PKOS_샘플/원본`, 결과는 `PKOS_샘플/변환결과`에 있습니다. 파일 우클릭 → 다운로드로 저장할 수 있습니다.

[샘플 PDF 보기](output/pdf/대한민국_태극기와_애국가.pdf) · [샘플 출처와 재생성 방법](samples/README.md)

현재 2부 PDF 변환은 **글자만 추출**하므로 태극기 그림은 결과 마크다운에 포함되지 않습니다. 샘플은 코랩 임시 공간에 저장되며 런타임 삭제 시 사라집니다. 공개 샘플의 원문 비교를 위해 샘플 셀만 개인정보 가리기를 끕니다.

### 폴더 경로 입력 예시

문서폴더: `/content/drive/MyDrive/00_개인지식운영체계(PKOS)/01_외부 정보`

결과폴더: `PKOS/변환결과`

기존에 복사해둔 PKEMS 노트북은 자동 업데이트되지 않습니다. 위 배지로 새 PKOS 노트북을 열고 준비하기 두 셀부터 실행하세요.

### 네이버 블로그 백업 방법 (1부를 쓸 경우)

블로그 관리 → 글 전체보기 → **인쇄** → 대상을 **'PDF로 저장'**
(한 번에 100편 정도씩 나눠 저장하면 안정적입니다)

---

## 🔒 개인정보 자동 가리기

업무 문서에는 이름·연락처·주소·계좌번호가 들어 있는 경우가 많습니다.
변환할 때 **자동으로 찾아 가립니다.** (기본값: 켜짐)

| 방식 | 예시 |
|------|------|
| **부분가림** | 홍길동 → `홍**` · 010-1234-5678 → `010-****-****` |
| **가림** | 900101-1234567 → `******-*******` |
| **삭제** | (빈칸으로) |
| **그대로** | 건드리지 않음 |

찾는 것: 주민등록번호 · 전화번호 · 이메일 · 계좌번호 · 카드번호 · 이름 · 주소 · 생년월일 · 차량번호

종류마다 방식을 따로 고를 수 있고, **이름 찾는 강도**도 3단계로 조절합니다.

### 변환 후 보고서

`_개인정보_보고서.md` 가 만들어집니다.
**어느 파일에서 무엇이 어떻게 바뀌었는지** 원본·변환값·확신도·주변 문맥까지 기록됩니다.

> ⚠️ **이 보고서에는 가리기 전 원본이 그대로 들어 있습니다.**
> 확인 후 삭제하거나, 절대 공유하지 마세요.
> (`보고서_원본표시`를 끄면 원본 없이 건수만 남깁니다)

### 정확도 — 솔직하게

실제 학교 업무 문서로 시험하며 다듬은 결과입니다.

| | 결과 |
|---|---|
| 초기 | 823건 탐지 — **대부분 오탐** (날짜를 계좌번호로, `학부모 안내`의 `안내`를 이름으로) |
| 개선 후 | **120건 · 이름 7명 전원 실제 인물, 오탐 0건** |

무엇을 고쳤나:

- 계좌번호는 **'계좌' 표시가 가까이 있을 때만** 인정 → 날짜 오탐 제거
- 이름 라벨을 **강·약으로 구분** — `학생`·`학부모` 같은 약한 표시는 3글자 이름만
- 조사로 끝나는 낱말 제외 (`성장을`, `지정및`)
- 띄어쓴 이름은 **모든 글자가 떨어진 형태만** 인정 → `이제 곧` 오탐 제거
- 주민등록번호는 검증식으로 **걸러내지 않음** — 오타 있는 진짜 번호를 놓치는 쪽이 더 위험
- 문서 작성일(`2025.5.20`)을 생년월일로 오인하지 않음

**그래도 완벽하지 않습니다.** 특이한 형식은 놓칠 수 있고, 문장 속 이름은 잡기 어렵습니다.
보고서를 만드는 이유가 바로 **사람이 최종 확인**하도록 하기 위해서입니다.

**공개 전에는 반드시 직접 점검하세요.**

---

## 내 자료는 어디로 가나

**아무 데도 가지 않습니다.**

- 코랩은 **본인 구글 계정**에서 실행됩니다
- 파일은 **본인 드라이브 안에서만** 읽고 씁니다
- 만든 사람의 서버는 **존재하지 않습니다**
- 코드는 전부 이 저장소에 공개되어 있습니다

---

## 결과물

**1부 (블로그)**
```
블로그백업/
├── 1_100.pdf …
└── md/
    ├── 2018-04-17_협동학습연수.md
    ├── images/<글제목>/img_001.jpeg
    ├── INDEX.md          연도별 목차
    └── _index.json
```

**2부 (문서 폴더)** — 원본 폴더 구조를 그대로 유지
```
변환결과/
├── (완료)/2025. 연수/강사승낙서_hwp.md
├── INDEX.md              형식별 목차
├── _files.json
├── _오류.md               실패 목록과 이유
└── _개인정보_보고서.md      무엇이 어떻게 가려졌는지
```

각 `.md` 머리에는 제목·날짜·출처·종류(·원문주소)가 들어갑니다.

---

## 저장소 구성

| 파일 | 역할 |
|------|------|
| `PKOS_변환기.ipynb` | **배포용 코랩 노트북** — 엔진이 통째로 들어 있어 이 파일 하나로 동작 |
| `pkos_converter.py` | 블로그 백업 PDF → md (글 단위 분리) |
| `pkos_readers.py` | 형식별 읽기 (한글·워드·PPT·엑셀·HTML…) |
| `pkos_privacy.py` | 개인정보 탐지·가리기·보고서 |
| `pkos_folder.py` | 폴더 일괄 변환 |
| `pkos_gdrive.py` | 구글 문서 내보내기 |
| `build_notebook.py` | 엔진들을 노트북에 다시 심는 스크립트 |

> **엔진을 고쳤으면 반드시 실행하세요.** 노트북 안에 엔진이 복사되어 들어가므로 자동 반영되지 않습니다.
> ```bash
> python build_notebook.py
> ```

---

## 로컬에서 직접 쓰기

```bash
pip install -r requirements.txt
```

```python
# 블로그 백업 PDF
from pkos_converter import Converter, Settings, inspect
inspect("백업/1_100.pdf")                      # 먼저 확인
Converter(Settings(pdf_dir="백업", out_dir="백업/md")).run()

# 문서 폴더 통째로
from pkos_folder import FolderConverter, FolderSettings
from pkos_privacy import Policy
fc = FolderConverter(FolderSettings(
    src_dir="문서폴더", out_dir="변환결과",
    개인정보_정책=Policy(이름="부분가림", 계좌번호="가림"),
))
fc.scan()          # 현황만 확인
fc.run(limit=30)   # 30개만 시험
fc.run()           # 전체

# 파일 하나만
from pkos_readers import read_any
r = read_any("보고서.hwp")
print(r.ok, r.kind, r.chars)
```

---

## 기술 메모

**블로그 PDF 글 경계**
`YYYY/MM/DD HH:MM` + `blog.naver.com/<아이디>/<글번호>` 두 줄 연속 패턴.

**자동 감지** — 블로그마다 다른 값을 하드코딩하지 않습니다.
- 블로그 아이디 → 정규식 추출
- 페이지 푸터(`12 · 블로그이름`) → 표본 페이지에서 **반복 빈도**로 블로그명을 추론해 제거

**한글 `.hwp`** — 순수 파이썬 처리
- CFB(OLE) 컨테이너 → `BodyText/Section*` 스트림 → raw deflate 해제
- 레코드 순회 중 `HWPTAG_PARA_TEXT`(0x43)만 추출
- 문단 텍스트는 UTF-16 **코드 단위** 배열이라, 표·그림 제어문자는 8워드 블록째 건너뛰고
  이모지는 **서로게이트 쌍**을 합쳐야 함 (안 하면 저장 시 오류)

**한글 `.hwpx`** — ZIP + XML. 표준 라이브러리만으로 처리.

**표 복원** — 학교 문서는 절반 이상이 표 위주라, 표를 살리지 않으면 칸이 한 줄씩
세로로 늘어져 읽을 수 없게 된다. 그래서 표를 **마크다운 표로 되살린다.**

- `.hwp` : `CTRL_HEADER('tbl ')` → `TABLE`(행·열 수) → 셀마다 `LIST_HEADER`
  셀 좌표는 `LIST_HEADER` **8번째 바이트**부터 (열·행·열병합·행병합, 각 UINT16)
- `.hwpx` : `<hp:tbl rowCnt colCnt>` 안의 `<hp:tc>` + `<hp:cellAddr>`
- `LIST_HEADER` 는 **글상자에도 쓰이므로**, 표가 선언한 크기를 벗어난 좌표는 셀이 아닌 것으로 본다
  (이 검사를 빼면 빈 칸이 폭증해 한 문서가 300만 자까지 부풀었다)
- 칸이 20,000개를 넘으면 표 대신 내용만 풀어쓴다

**구글 문서** — 로컬에 실체가 없는 온라인 문서라 Drive API 로 내보내기 필요.
- 문서 → 마크다운으로 바로 내보냄
- 시트 → CSV 로 받으면 **첫 장만** 오므로 `.xlsx` 로 받아 **모든 시트**를 표로 옮김
- 슬라이드 → 텍스트로 받으면 **발표자 노트가 빠지므로** `.pptx` 로 받아 노트까지 옮김
- 세 가지 모두 최종 결과는 `.md` 하나로 통일

---

## 알려진 한계

| 한계 | 내용 |
|------|------|
| 서식 손실 | 표는 마크다운 표로 살리지만, 병합된 칸은 첫 칸에만 값이 들어감. 굵게·색·그림 위치는 단순화됨 |
| **스캔한 문서** | **변환할 수 없습니다** (아래 참고) |
| **옛 오피스 형식** | `.xls` `.ppt` `.doc` **미지원** (아래 참고) |
| 구형 `.hwp` 예외 | 암호 걸린 문서, 배포용 문서는 실패할 수 있음 |
| 처리 시간 | 100MB당 대략 1~3분 |
| 이미지 용량 | 사진이 많으면 결과가 원본보다 커질 수 있음 |

변환 전 **미리 확인(scan / inspect)** 으로 규모를 먼저 파악하는 것을 권합니다.

### 스캔한 문서는 변환되지 않습니다

종이를 스캔하거나 사진으로 찍어 만든 PDF·이미지는 **글자가 아니라 그림**입니다.
읽어내려면 글자 인식(OCR)이 필요한데, 이 도구는 **OCR을 하지 않습니다.**

- 이런 파일은 변환하지 않고 `_오류.md` 에 이유가 남습니다
- **원본 문서 파일(한글·워드 등)이 있으면 그것을 변환하세요** — 결과가 훨씬 좋습니다

### 옛 오피스 형식은 새 형식으로 저장한 뒤 쓰세요

`.xls` `.ppt` `.doc` 는 지금 형식과 구조가 완전히 달라 다루지 않습니다.
바꾸는 건 간단합니다.

| 옛 형식 | 이렇게 |
|---------|--------|
| `.xls` | 엑셀에서 열기 → **다른 이름으로 저장** → `.xlsx` |
| `.ppt` | 파워포인트에서 열기 → **다른 이름으로 저장** → `.pptx` |
| `.doc` | 워드에서 열기 → **다른 이름으로 저장** → `.docx` |

> **확장자만 바꾸는 것으로는 안 됩니다.** 파일 이름을 `.xls` → `.xlsx` 로 고쳐도
> 속은 그대로라 변환되지 않습니다. 반드시 프로그램에서 **다시 저장**하세요.
> (이 경우도 알아채서 `_오류.md` 에 안내가 남습니다)

---

## 함께 만들기

버그 제보와 개선 제안을 환영합니다. [Issues](../../issues)에 남겨주세요.

특히 도움이 되는 제보:
- **놓친 개인정보** 또는 **잘못 가려진 일반 낱말** (원본은 지우고 형태만 알려주세요)
- 변환에 실패한 문서의 **종류와 오류 메시지**

---

## 라이선스

[MIT](LICENSE) · 자유롭게 쓰고 고치고 나눠 쓰세요.

---

*모두가 자신의 지식·경험을 스스로 관리하는 체계를 갖도록.
기술적 장벽이 낮은 범위(적정지식) 안에서 만들고 나눕니다.*
